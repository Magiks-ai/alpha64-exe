# Copy to .env.local or .local-secrets/bitquery.env and fill locally.
# Never commit real secrets.
BITQUERY_API_KEY=
# Optional after Worker deploy:
# ALPHA64_BITQUERY_PROXY=https://alpha64-bitquery-dex.<account>.workers.dev/
